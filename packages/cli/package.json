{
  "name": "@maia/cli",
  "version": "0.1.0",
  "description": "Maia CLI — stream, replay, and validate ACP agent events from the terminal",
  "bin": {
    "maia": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/cli.ts --format cjs --target node18 --clean",
    "dev": "tsup src/cli.ts --format cjs --target node18 --watch"
  },
  "dependencies": {
    "@maia/acp": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "keywords": ["ai", "agent", "cli", "acp", "maia"],
  "author": "Maia AI",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/maia-ai/maia-sdk"
  }
}
